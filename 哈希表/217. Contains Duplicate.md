當不需要額外資訊的時候 使用set 需要額外資訊使用map

```cpp
class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        unordered_set<int>set(nums.begin(),nums.end());
        return set.size()<nums.size();
    }

    // set 解法1
    // bool containsDuplicate(vector<int>& nums) {
    //     unordered_set<int>set;
    //     for(int num:nums){
    //         if(set.count(num)!=0)return true;
    //         set.insert(num);
    //     }
    //     return false;
    // }
};
```
